---
const testimonials = [
    {
        text: `"As the sole user, I can confidently say: FORGOR is absolutely game-changing. 10/10, would recommend to myself again."`,
        author: "— Nikhil, Creator & Most Dedicated User"
    },
    {
        text: `"Love FORGOR's daily digest and recommendations!"`,
        author: "— Another User"
    }
];
---

<section class="z-20 w-full max-w-6xl mt-20 text-center">
    <h2 class="text-4xl font-display mb-8">What Users Are Saying</h2>
    <div class="flex flex-col md:flex-row justify-center md:gap-10 gap-8">
        {testimonials.map(({ text, author }) => (
        <div class="flex flex-col items-center p-6 bg-purple-800 w-full md:flex-1">
            <p class="text-xl italic mb-4">{text}</p>
            <p class="font-bold text-neonGreen-DEFAULT text-lg">{author}</p>
        </div>
        ))}
    </div>
</section>
