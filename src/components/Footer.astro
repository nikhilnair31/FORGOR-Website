---
// src/components/Footer.astro
const currentPath = Astro.url.pathname;
---

<footer class="border-t border-neonGreen py-6 text-sm text-white">
    <div class="max-w-6xl mx-auto flex flex-col md:flex-row justify-between items-center md:items-start gap-4 px-4">
        <!-- Left side -->
        <div class="flex flex-col md:flex-row md:items-center gap-3 opacity-80">
        <p>© FORGOR {new Date().getFullYear()}</p>
        </div>

        <!-- Right side -->
        <nav class="flex space-x-4">
            <a href="/privacy" class={`${currentPath === '/privacy/' ? 'underline' : ''} hover:underline`}>
                Privacy
            </a>
            <a href="/contact" class={`${currentPath === '/contact/' ? 'underline' : ''} hover:underline`}>
                Contact
            </a>
            <a href="/faq" class={`${currentPath === '/faq/' ? 'underline' : ''} hover:underline`}>
                FAQ
            </a>
        </nav>
    </div>
</footer>
